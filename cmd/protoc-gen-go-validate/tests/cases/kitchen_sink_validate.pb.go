// Code generated by protoc-gen-go-validate. DO NOT EDIT.
// - protoc-gen-go-validate 1.0.0
// - protoc             v6.30.2
// source: cases/kitchen_sink.proto

package cases

import (
	"bytes"
	"errors"
	"fmt"
	"net"
	"net/url"
	"regexp"
	"sort"
	"strings"
	"time"
	"unicode/utf8"

	"github.com/ml444/gkit/errorx"
)

// ensure the imports are used
var (
	_ = bytes.MinRead
	_ = errors.New("")
	_ = fmt.Print
	_ = utf8.UTFMax
	_ = (*regexp.Regexp)(nil)
	_ = (*strings.Reader)(nil)
	_ = net.IPv4len
	_ = time.Duration(0)
	_ = (*url.URL)(nil)
	_ = sort.Sort
)

func kitchen_sinkValidationError(errCode int32, msg string, cause error) error {
	err := errorx.CreateError(400, errCode, msg)
	if cause != nil {
		err = err.WithCause(cause)
	}
	return err
}

// kitchen_sinkMultiError is an error wrapping multiple validation errors
// returned by .ValidateAll() if the designated constraints aren't met.
type kitchen_sinkMultiError []error

// Error returns a concatenation of all the error messages it wraps.
func (m kitchen_sinkMultiError) Error() string {
	var msgs []string
	for _, err := range m {
		msgs = append(msgs, err.Error())
	}
	return strings.Join(msgs, "; ")
}

// AllErrors returns a list of validation violation errors.
func (m kitchen_sinkMultiError) AllErrors() []error { return m }

func (m *ComplexTestMsg) Validate() error {
	return m.validate(false)
}

func (m *ComplexTestMsg) ValidateAll() error {
	return m.validate(true)
}

func (m *ComplexTestMsg) validate(all bool) error {
	if m == nil {
		return nil
	}
	var errors []error
	if m.GetConst() != "abcd" {
		err := kitchen_sinkValidationError(
			0,
			fmt.Sprintf("[Const] value must equal %s", "abcd"),
			nil,
		)
		if !all {
			return err
		}
		errors = append(errors, err)
	}

	if m.GetIntConst() != 5 {
		err := kitchen_sinkValidationError(
			0,
			fmt.Sprintf("[IntConst] value must equal %s", 5),
			nil,
		)
		if !all {
			return err
		}
		errors = append(errors, err)
	}

	if m.GetBoolConst() != false {
		err := kitchen_sinkValidationError(
			0,
			fmt.Sprintf("[BoolConst] value must equal %s", false),
			nil,
		)
		if !all {
			return err
		}
		errors = append(errors, err)
	}

	if wrapper := m.GetFloatVal(); wrapper != nil {

		if wrapper.GetValue() <= 0 {
			err := kitchen_sinkValidationError(
				0,
				"[FloatVal] value must be greater than 0",
				nil,
			)
			if !all {
				return err
			}
			errors = append(errors, err)
		}

	}

	if m.GetDurVal() == nil {
		err := kitchen_sinkValidationError(
			0,
			"[DurVal] value is required",
			nil,
		)
		if !all {
			return err
		}
		errors = append(errors, err)
	}

	if d := m.GetDurVal(); d != nil {
		dur, err := d.AsDuration(), d.CheckValid()
		if err != nil {
			err := kitchen_sinkValidationError(
				0,
				"[DurVal] value is not a valid duration",
				nil,
			)
			if !all {
				return err
			}
			errors = append(errors, err)
		} else {
			lt := time.Duration(17*time.Second + 0*time.Nanosecond)
			if dur >= lt {
				err := kitchen_sinkValidationError(
					0,
					"[DurVal] value must be less than 17s",
					nil,
				)
				if !all {
					return err
				}
				errors = append(errors, err)
			}

		}
	}

	if t := m.GetTsVal(); t != nil {
		ts, e := t.AsTime(), t.CheckValid()
		if e != nil {
			err := kitchen_sinkValidationError(
				0,
				"[TsVal] value is not a valid timestamp",
				e,
			)
			if !all {
				return err
			}
			errors = append(errors, err)
		} else {
			gt := time.Unix(7, 0)
			if ts.Sub(gt) <= 0 {
				err := kitchen_sinkValidationError(
					0,
					"[TsVal] value must be greater than 1970-01-01 00:00:07 +0000 UTC",
					nil,
				)
				if !all {
					return err
				}
				errors = append(errors, err)
			}

		}
	}

	if m.GetFloatConst() >= 8 {
		err := kitchen_sinkValidationError(
			0,
			"[FloatConst] value must be less than 8",
			nil,
		)
		if !all {
			return err
		}
		errors = append(errors, err)
	}

	if _, ok := _ComplexTestMsg_DoubleIn_InLookup[m.GetDoubleIn()]; !ok {
		err := kitchen_sinkValidationError(
			0,
			"[DoubleIn] value must be in [456.789 123]",
			nil,
		)
		if !all {
			return err
		}
		errors = append(errors, err)
	}

	if m.GetEnumConst() != 2 {
		err := kitchen_sinkValidationError(
			0,
			"[EnumConst] value must equal ComplexTWO",
			nil,
		)
		if !all {
			return err
		}
		errors = append(errors, err)
	}

	if a := m.GetAnyVal(); a != nil {
		if _, ok := _ComplexTestMsg_AnyVal_InLookup[a.GetTypeUrl()]; !ok {
			err := kitchen_sinkValidationError(
				0,
				"[AnyVal] type URL must be in [type.googleapis.com/google.protobuf.Duration]",
				nil,
			)
			if !all {
				return err
			}
			errors = append(errors, err)
		}
	}

	for idx, item := range m.GetRepTsVal() {
		_, _ = idx, item

		if t := item; t != nil {
			ts, e := t.AsTime(), t.CheckValid()
			if e != nil {
				err := kitchen_sinkValidationError(
					0,
					"[RepTsVal] value is not a valid timestamp",
					e,
				)
				if !all {
					return err
				}
				errors = append(errors, err)
			} else {
				gte := time.Unix(0, 1000000)
				if ts.Sub(gte) < 0 {
					err := kitchen_sinkValidationError(
						0,
						"[RepTsVal] value must be greater than or equal to 1970-01-01 00:00:00.001 +0000 UTC",
						nil,
					)
					if !all {
						return err
					}
					errors = append(errors, err)
				}

			}
		}

	}

	{
		if all {
			sorted_keys := make([]int32, len(m.GetMapVal()))
			i := 0
			for key := range m.GetMapVal() {
				sorted_keys[i] = key
				i++
			}
			sort.Slice(sorted_keys, func(i, j int) bool { return sorted_keys[i] < sorted_keys[j] })
			for _, key := range sorted_keys {
				val := m.GetMapVal()[key]
				_ = val

				if key >= 0 {
					err := kitchen_sinkValidationError(
						0,
						"[MapVal] value must be less than 0",
						nil,
					)
					if !all {
						return err
					}
					errors = append(errors, err)
				}

				// no validation rules for MapVal

			}
		} else {
			for key, val := range m.GetMapVal() {
				_, _ = key, val

				if key >= 0 {
					err := kitchen_sinkValidationError(
						0,
						"[MapVal] value must be less than 0",
						nil,
					)
					if !all {
						return err
					}
					errors = append(errors, err)
				}

				// no validation rules for MapVal

			}
		}
	}

	if !bytes.Equal(m.GetBytesVal(), []uint8{0x0, 0x99}) {
		err := kitchen_sinkValidationError(
			0,
			"[BytesVal] value must equal [0 153]",
			nil,
		)
		if !all {
			return err
		}
		errors = append(errors, err)
	}

	oneofOPresent := false
	switch v := m.O.(type) {
	case *ComplexTestMsg_X:
		if v == nil {
			err := kitchen_sinkValidationError(
				0,
				"[O] oneof value cannot be a typed-nil",
				nil,
			)
			if !all {
				return err
			}
			errors = append(errors, err)
		}
		oneofOPresent = true
		// no validation rules for X
	case *ComplexTestMsg_Y:
		if v == nil {
			err := kitchen_sinkValidationError(
				0,
				"[O] oneof value cannot be a typed-nil",
				nil,
			)
			if !all {
				return err
			}
			errors = append(errors, err)
		}
		oneofOPresent = true
		// no validation rules for Y
	default:
		_ = v // ensures v is used
	}
	if !oneofOPresent {
		err := kitchen_sinkValidationError(
			0,
			"[O] value is required",
			nil,
		)
		if !all {
			return err
		}
		errors = append(errors, err)
	}

	if len(errors) > 0 {
		return kitchen_sinkMultiError(errors)
	}
	return nil
}

var _ComplexTestMsg_DoubleIn_InLookup = map[float64]struct{}{
	456.789: {},
	123:     {},
}
var _ComplexTestMsg_AnyVal_InLookup = map[string]struct{}{
	"type.googleapis.com/google.protobuf.Duration": {},
}
